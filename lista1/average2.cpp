#include <bits/stdc++.h>

int main() {
  double A, B, C;
  std::cin >> A >> B >> C;
  std::cout.precision(1);
  std::cout << std::fixed;
  std::cout << "MEDIA = " << (2.0 * A + 3.0 * B + 5.0 * C) / 10.0 << std::endl;
  return 0;
}
